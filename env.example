# Database (Banco externo - configure com as credenciais do seu banco)
# Se rodando no Docker e o banco estiver no host:
#   Windows/Mac: use host.docker.internal
#   Linux: use o IP da sua máquina ou configure network_mode: host no docker-compose
# Exemplo: DB_HOST=host.docker.internal ou DB_HOST=192.168.1.100
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=rh360
DB_SYNCHRONIZE=false
DB_LOGGING=false

# JWT
JWT_SECRET=Rh360SecretKeyForJWTTokenGeneration2024SecureAndLongEnough
JWT_EXPIRATION=86400000
JWT_QRCODE_EXPIRATION=900000

# Application
PORT=8080
NODE_ENV=development

# Cloudflare R2
CLOUDFLARE_R2_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=rh360
CLOUDFLARE_R2_ENDPOINT_URL=
CLOUDFLARE_R2_PUBLIC_URL=

# CompreFace
COMPREFACE_API_URL=http://localhost:8000
COMPREFACE_API_KEY=

# Frontend
APP_FRONTEND_URL=http://localhost:3000
APP_QRCODE_WIDTH=300
APP_QRCODE_HEIGHT=300

# Redis (Redis externo - configure com as credenciais do seu Redis, se necessário)
REDIS_HOST=localhost
REDIS_PORT=6379
